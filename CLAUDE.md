# CLAUDE.md

## Agent skills

### Issue tracker

Issues live in GitHub Issues (`keboh13/golf-strategy-engine`), accessed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical role names used verbatim as label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
